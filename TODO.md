# TODO

## Step 1
- Kiểm tra vì sao `localhost:3000` báo `Cannot GET`.
- Đối chiếu `server.js` với các file router (đặc biệt `routes/pageRouters.js`).

## Step 2
- Đã sửa: gắn `pageRouters` vào `server.js` để route `/` trả về `pages/login.html`.
- Kiểm tra `http://localhost:3000/` trả về HTTP 200.


## Step 3
- (Nếu cần) Chuẩn hóa lại `routes/authRoutes.js` endpoint để frontend gọi đúng.

## Step 4
- Chạy lại server và kiểm tra bằng browser/Postman.
dashboard 2 container col8 col4
inddex
